{
  "name": "autocomplete-linked-field",
  "version": "2.0.0",
  "description": "Allows for the autocompletion of entries from like-named fields in Contenful UI.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create": "contentful-extension create --space-id $CONTENTFUL_API_SPACE",
    "update": "contentful-extension update --space-id $CONTENTFUL_API_SPACE --force",
    "dev": "contentful-extension update --space-id $CONTENTFUL_API_SPACE --force --src 'http://localhost:3030/index.html'"
  },
  "author": "s@ndyroge.rs",
  "license": "MIT",
  "devDependencies": {
    "contentful-extension-cli": "^2.0.0"
  },
  "dependencies": {
  }
}
